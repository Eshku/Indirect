{
  "name": "pixi-2d-hero-defense-multiplayer",
  "version": "0.1.7",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "set NODE_ENV=development&& electron .",
    "build": "set NODE_ENV=production&& electron-builder --win",
    "bundle": "echo \"Error: no bundle specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^35.1.1",
    "electron-builder": "^26.0.12",
    "electron-reload": "^2.0.0-alpha.1"
  },
  "dependencies": {
    "install": "^0.13.0",
    "pixi.js": "^8.9.1",
    "planck": "^1.4.2"
  }
}
