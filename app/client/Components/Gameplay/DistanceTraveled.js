/**
 * A component that tracks the distance an entity has traveled since it was spawned.
 */
export const DistanceTraveled = {
	/**
	 * The total distance traveled.
	 */
	value: {
		type: 'f32',
		default: 0,
	},
}
