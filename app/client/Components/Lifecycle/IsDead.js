/**
 * A tag component that marks an entity as "dead" from a gameplay perspective,
 * making it inactive and ready for reuse from a pool. This is a performant
 * alternative to destroying and recreating high-turnover entities like particles.
 */
export class IsDead {}
