/**
 * A tag component used in the Hybrid Pooling Pattern.
 * Entities with this tag are considered inactive and part of a pool,
 * excluded from most game logic systems.
 */
export const isPooled = {}