/**
 * A tag component that explicitly marks an entity as being able to receive damage.
 * Systems like DamageSystem use this tag in their queries to clearly identify their targets.
 */
export const damageable = {}