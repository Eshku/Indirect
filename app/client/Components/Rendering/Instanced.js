/**
 * A tag component indicating that this entity should be rendered by the
 * InstancedRenderSystem for high-performance batching, rather than the
 * standard SpriteFactorySystem.
 */
export const Instanced = {}