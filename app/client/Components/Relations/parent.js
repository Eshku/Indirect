/**
 * A component that creates a parent-child relationship between entities.
 */
export const parent = {
	/**
	 * The entity ID of the parent.
	 */
	entityId: {
		type: 'entity',
		default: 0n,
	},
}

