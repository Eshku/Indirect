/**
 * A tag component that marks an entity as an active effect that needs to be
 * processed by the `EffectExecutionSystem`. This entity acts as a transient
 * "message" carrying all data needed to resolve an action.
 */
export class ActiveEffect {
}